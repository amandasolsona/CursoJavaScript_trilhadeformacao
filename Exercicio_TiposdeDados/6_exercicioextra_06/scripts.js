/*Crie uma variável chamada isActive com o valor booleano de true;
Imprima com console.log o valor da variável */

/*let é uma palavra-chave em JS usada para declarar variáveis. Ela permite que você crie uma variável
 com escopo de bloco, o que significa que a variávelsó pode ser acessada dentro do bloco de código
  em que foi declarada. let # var */

let isActive = true;
console.log(isActive);
